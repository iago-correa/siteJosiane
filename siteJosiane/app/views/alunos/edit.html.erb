<div style="width:70%;padding:10px;">
	
	<h1>Editar aluno</h1>

	<hr><br>

	<% if @msg %>
		<%= @msg %> 
	<%end%>

	<% if @aluno.errors.any? %>

	    <% @aluno.errors.full_messages.each do |m| %>

	        <%= m %> <br>

	    <% end %>

	<% end %>

	<br>

	<center>

		<%= form_for @aluno do |f| %>
			Nome: <%= f.text_field :nome %><br/>
			E-mail: <%= f.text_field :email %><br/>
			Matrícula: <%= f.text_field :matricula %><br/>
			Turma: <%= collection_select(:aluno, :turma_id, Turma.all.order(:codigo), :id, :codigo) %>
			<br>
			<%= link_to "Cancelar", root_path(:index), class:"button" %> 
			<%= submit_tag 'Cadastrar' %>
		<% end %>

	</center>

</div>

